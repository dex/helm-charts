apiVersion: v1
kind: Service
metadata:
  name: {{ include "yacreader-server.fullname" . }}
  labels:
    {{- include "yacreader-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    {{- include "yacreader-server.selectorLabels" . | nindent 4 }}
